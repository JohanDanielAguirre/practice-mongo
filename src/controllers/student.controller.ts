import {Request, Response} from 'express';

class StudentController {
    async getAll(req: Request, res: Response) {
        try {
            //crear un service
        }catch(err) {
            console.log(err);
        }
    }
}